<?php
// robots.php - Generates dynamic robots.txt content
// In a real production environment, you might use URL rewriting to serve this as robots.txt
// Or simply save this content to a static robots.txt file. 
// For this environment, we will create a static robots.txt file.

$content = "User-agent: *\n";
$content .= "Allow: /\n";
$content .= "Disallow: /admin/\n";
$content .= "Disallow: /super_admin/\n";
$content .= "Disallow: /owner/\n";
$content .= "Disallow: /customer/\n";
$content .= "Disallow: /includes/\n";
$content .= "Disallow: /config/\n";
$content .= "Sitemap: " . app_url('sitemap.php') . "\n"; // Assuming we create sitemap.php

// We can write this to a file or output it directly.
// Since we are in an agent workflow, let's write to the file directly using the tool.
?>
